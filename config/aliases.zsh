alias cp='cp -i'
alias mv='mv -i'
alias cat='bat'
alias rm='trash -v'
alias mkdir='mkdir -p'
alias ps='ps auxf'

# Remove a directory and all files
alias rmd='/bin/rm  --recursive --force --verbose '
